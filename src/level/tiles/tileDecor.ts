import * as THREE from "three";
import type { PlacedTile } from "../LevelTypes";

export function addSparseDecor(_root: THREE.Object3D, _tile: PlacedTile): void {
  // Intentionally empty: keep procgen maps visually clean and let FBX tiles carry the art.
}
